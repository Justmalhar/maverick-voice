# Dev-only key seed (read when no encrypted key is stored AND app is unpackaged).
# Copy to .env — never committed.
GROQ_API_KEY=
OPENAI_API_KEY=
OPENROUTER_API_KEY=
DEEPGRAM_API_KEY=
CUSTOM_API_KEY=
